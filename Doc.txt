[Trigger-Controller] 

    [Pipes]
    + transformation: chuyển đổi kiểu dữ liệu, ex: "7" --parse--> 7 number
    + validation: validate client gửi lên có hợp lệ hay không

    [Client] ----> [Pipes- thành phần trung gian] hợp lệ ---mới đưa vào---> [Controller]

        [Pipes] :
            + params
            + Controller
            + global

[Controller]: receive data input, return data output
[Service]: nơi xử lý dữ liệu, query đến db như thế nào?, xử lý logic  

    + class Validator
    + class Transform: 
        => nếu user gửi lên field dư thừa làm sao ? không validate được 
        => làm sao pipes trước khi vào db

        plainToClass: chuyển đổi `user` obj plain Javascript thành đối tượng thuộc lớp `UserDto`

        + BaseDto: export abstract class BaseDto -- là lớp trừ tượng thường được chia sẻ cho các lớp khác
Dependency: 
    [Controller] --> [Service] --> [Repository]

    => có nhiều thằng query lộn xộn => bị rối (không nên xài)

Nên sử dụng Repository @Injection

+ Mỗi [Module] có một Injector riêng: 
    + global Injector 
    + Module Injector 


    readonly: không muốn sửa bất cứ gì cả 

    Hiện đang provider: ClassName
    + Giả sử: tải 1 src trên mạng đã có [Module] rồi, viết sẵn hết rồi
    => [Module] của mình bị conflict => nên sử dụng Key để xử lý

+ 3 cấp độ Inject 
    - Injectable -- thông báo cho nestjs biết đây là đối tượng được Inject 
    - Inject
    - 

    Nếu thay key 'USER_SERVICE_QUANNA' thành 'APP_USER' in [users.controller.ts]
    => sẽ báo lỗi, => do mỗi [Module] có 1 Injector riêng, dù 'APP_USER' gọi UserService đi chăng nữa



